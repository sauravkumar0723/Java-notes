### Abstraction
--> ABSTRACTIOn in java is the concept of hiding the internal details of a system and showing only the essential features.
--> it allow you to simplify complex system by focusing only on what is important, without worrying about how thing swork behind the scenes.

Why is Abstraction Important?

1. Simplifies Complexity:
Abstraction helps you focus on important parts, hiding the complicated details. It's like using an app without needing to know how it works.
2. Hides Unnecessary Details:
Just like driving a car without knowing how the engine works, abstraction hides the complexity and shows only what's necessary.
3. Reusability:
It allows you to create general templates that can be used in different places, saving time and effort.
4. Easy Updates:
Abstraction makes it easy to update parts of the system without breaking other parts. It's like updating an app on your phone without affecting how you use it.
Q Real-World Applications of Abstraction

1. Smartphone Apps: Using apps like Instagram, you don't need to know how they work; you just enjoy posting or messaging.
2. Video Games: You play the game without worrying about how graphics or the game engine work.

Note:-> if you have incomplete method then i will make you as abstract.
2. -> Complete method means you should give the body

if the class is containig , not2m if the class is containing 1abstract method, immediately you make that class as abstract.

note :- abstract void disp1() -> these method are technically called abstract method. also called incomplete method
--> void display()
{
// complete method because we have give the body. these method are called concrete method.
}

abstract class Parent {
	abstract void disp1();
	abstract void disp2();
}
public class Child1 extends Parent{
	void disp1()
	{
		System.out.println("inside child1 disp1");
	}
	void disp2()
	{
		System.out.println("inside child1 disp2");
	}
}
public class Child2 extends Parent{
	void disp1()
	{
		System.out.println("inside child2 disp1");
	}
	void disp2()
	{
		System.out.println("inside child2 disp2");
	}
}

public class Main {
	public static void main(String[] args) {
		Child1 c1 = new Child1();
		Child2 c2 = new Child2();
		
		acessMethod(c1);
		acessMethod(c2);
		
	}
	public static void acessMethod(Parent ref)
	{
		//ref.disp1(); // problem occured if we removed parent class method. so Parent is feeling bad no one is using
		// due to Problem occured specialized method of child()
	
		// ref.disp2(); //
		// i wnat to resolve the error to provide downcasting
		if(ref instanceof Child1)
		{
			((Child1)ref).disp1();
			((Child1)ref).disp2();
		} else {
			((Child2)ref).disp2();
			((Child2)ref).disp2();
		}
	}
}
